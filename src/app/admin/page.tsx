import React from "react";
import { AppSidebar } from "@/components/app-sidebar";
const page = () => {
  return (
    <div>
      <h1>Admin Page</h1>
      <AppSidebar />
    </div>
  );
};

export default page;
